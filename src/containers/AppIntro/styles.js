import {
    StyleSheet,

} from 'react-native';
const styles = StyleSheet.create({
    text: {
        // marginTop: 40
        fontSize: 20
    },
    skip: {
        fontSize: 20
    }
});
export default styles